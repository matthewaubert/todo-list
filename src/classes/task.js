export default class Task {
  constructor(name, dueDate, priority, notes, checkList) {
    this.name = name;
    this.dueDate = dueDate;
    this.priority = priority;
    this.notes = notes;
    this.project = project;
    this.checkList = checkList;
  }
}